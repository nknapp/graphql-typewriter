type A {
  aName: String
}
type B {
  bName: String
}

# A union type consisting only of type A
union Single = A

# A union type that may be A or B
union AOrB = A | B

type Query {
  single: Single
  aOrB(a: Int): AOrB
}